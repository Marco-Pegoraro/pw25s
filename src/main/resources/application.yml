spring:
  datasource:
    generate-unique-name: false
  H2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    properties:
      javax:
        persistance:
          validation:
            mode: none
